'use client';

import SalesPOSPage from './sales/page';

/**
 * Root POS Billing View
 * Replaces the previous Overview page with the direct POS Billing workspace.
 */
export default function RootPage() {
  return <SalesPOSPage />;
}

